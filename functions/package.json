{
  "name": "puxar-dados-tiktok-functions",
  "version": "0.1.0",
  "description": "Cloud Functions leves: coleta agendada de produtos TikTok Shop BR + trigger do 'Coletar agora' do dashboard. Sem browser (Playwright vive em video-collector-service/, no Cloud Run — ver README).",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions,firestore",
    "shell": "firebase functions:shell",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^7.3.2"
  }
}
